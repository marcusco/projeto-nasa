spring:
  cloud:
    config:
      enabled: false
      uri: https://github.com/marcusco/projeto-nasa/nasa/config ##this is the link to the server config
      label: main ##this is what you are missing (name of the git repo branch)
  application:
    name: currency-spring-cloud-config-client
  profiles:
    active: testing


server:
  port: 8600